<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical" >

    <Button
        android:id="@+id/gps_activate_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/gps_activate_button"
        android:layout_alignParentTop="true" />
    
   <ImageView android:id="@+id/powered_by_google_image"
        android:layout_width="fill_parent"
        android:layout_height="26dip"
        android:src="@drawable/powered_by_google_on_non_white"
        android:contentDescription="Powered by Google"
        android:layout_alignParentBottom="true" />
   
    <ListView
        android:id="@+id/android:list"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_below="@id/gps_activate_button" 
        android:layout_above="@id/powered_by_google_image" >
    </ListView>

    <TextView android:id="@id/android:empty"
		android:layout_width="fill_parent" 
		android:layout_height="fill_parent" 
		android:text="No data"
		android:layout_below="@id/gps_activate_button" 
		android:layout_above="@id/powered_by_google_image" />
    
</RelativeLayout>